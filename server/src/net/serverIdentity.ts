export const identity = {
  clientId: 'colyseus-host',
  boatId: 'colyseus-host-boat',
  clientName: 'Colyseus Host',
}

export const setBoatId = (_nextBoatId: string) => {
  // no-op on server
}

export const setClientName = (_name: string) => {
  // no-op on server
}

